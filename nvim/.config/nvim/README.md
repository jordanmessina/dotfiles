# LazyVim

Starter template for [LazyVim](https://github.com/LazyVim/LazyVim).

After Stow links this package, run `nvim` to bootstrap plugins on first launch.
Then run `:LazyHealth` inside Neovim to verify the setup.
